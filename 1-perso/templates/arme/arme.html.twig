{% extends 'base.html.twig' %}

{% block title %}Armes{% endblock %}

{% block monTitre %}
Arme {{ arme.nom }}
{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-2">
            <img src="{{ asset('images/armes/' ~ arme.nom|replace({'é' : 'e'}) ~ '.png') }}">
        </div>
        <div class="col">
            <h2>{{ arme.nom}}</h2>
            <p>{{ arme.desc}}</p>
            <p>{{ arme.degats}}</p>
        </div>
    </div>
{% endblock %}
